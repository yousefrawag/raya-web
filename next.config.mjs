// next.config.js
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "upload.wikimedia.org",
      "via.placeholder.com",
      "images.ctfassets.net"
    ],
  },
};

export default nextConfig;
